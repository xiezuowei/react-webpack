import React from 'react';
import './styles/app.less';
import AppRouter from '@/layouts/components/AppRouter';

const App: React.FC = () => {
    return (
        <AppRouter />
    );
};

export default App;
