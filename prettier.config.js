module.exports = {
    // 一行最多120字符
    printWidth: 120,
    // 使用4个空格缩进
    tabWidth: 4,
    // tab不使用缩进符，而使用空格
    useTabs: false,
    // 行尾需要有分号
    semi: true,
    // 使用单引号
    singleQuote: true,
    // jsx不使用单引号，而使用双引号
    jsxSingleQuote: false,
    // 末尾不需要逗号
    trailingComma: 'none',
    // 箭头函数只有一个参数时也需要括号
    arrowParens: 'always',
    // 行尾换行符
    endOfLine: 'auto'
};
